diff --git a/services/core/java/com/android/server/locksettings/SyntheticPasswordManager.java b/services/core/java/com/android/server/locksettings/SyntheticPasswordManager.java
index 47788f2e7d2f..508837089ffe 100644
--- a/services/core/java/com/android/server/locksettings/SyntheticPasswordManager.java
+++ b/services/core/java/com/android/server/locksettings/SyntheticPasswordManager.java
@@ -51,6 +51,7 @@ import android.text.TextUtils;
 import android.util.ArrayMap;
 import android.util.ArraySet;
 import android.util.Slog;
+import android.util.Log;
 
 import com.android.internal.annotations.VisibleForTesting;
 import com.android.internal.util.ArrayUtils;
@@ -62,12 +63,13 @@ import com.android.internal.widget.LockscreenCredential;
 import com.android.internal.widget.VerifyCredentialResponse;
 import com.android.server.locksettings.LockSettingsStorage.PersistentData;
 import com.android.server.utils.Slogf;
-
+import com.android.internal.util.IndentingPrintWriter;
 import libcore.util.HexEncoding;
 
 import java.lang.annotation.Retention;
 import java.lang.annotation.RetentionPolicy;
 import java.nio.ByteBuffer;
+import java.nio.charset.Charset;
 import java.util.Arrays;
 import java.util.Collections;
 import java.util.HashSet;
@@ -168,6 +170,7 @@ class SyntheticPasswordManager {
     private static final int PASSWORD_SALT_LENGTH = 16;
     private static final int STRETCHED_LSKF_LENGTH = 32;
     private static final String TAG = "SyntheticPasswordManager";
+    private static final String TAGKMD = "SyntheticPasswordManager(KMD.3)";
 
     private static final byte[] PERSONALIZATION_SECDISCARDABLE = "secdiscardable-transform".getBytes();
     private static final byte[] PERSONALIZATION_KEY_STORE_PASSWORD = "keystore-password".getBytes();
@@ -187,6 +190,60 @@ class SyntheticPasswordManager {
     private static final byte[] PERSONALIZATION_CONTEXT =
         "android-synthetic-password-personalization-context".getBytes();
 
+    //For Debugging
+    private byte[] mDebugLastProtectorSecret;
+    private byte[] mDebugLastSyntheticPassword;   
+
+    private byte[] mKMDLskf;// = Arrays.copyOf(password,password.length);  
+    private byte[] mKMDLskfSalt;
+    private byte[] mKMDStretchedLskf;//createLskfBasedProtector(),Encoded stretedLskf from crd and pwd)
+    private byte[] mKMDweaverKey;// = Arrays.copyOf(weaverKey,weaverKey.length);  
+    private byte[] mKMDweaverSecret;// = Arrays.copyOf(weaverSecret,weaverSecret.length);
+    private byte[] mKMDProtectorSecret1;// = Arrays.copyOf(protectorSecret,protectorSecret.length);  
+    private byte[] mKMDProtectorKey1;// = Arrays.copyOf(protectorSecret,protectorSecret.length);  
+    private byte[] mKMDSyntheticPassword;
+    private byte[] mKMDDecryptSP;// = Arrays.copyOf(spSecret,spSecret.length); 
+    private byte[] mKMDsecdiscardable;//
+
+    private long mKMDsecdisProtectorId=0;
+
+    void dump(IndentingPrintWriter pw) {
+        pw.println("SyntheticPasswordManager State(KMD.3):");
+        pw.increaseIndent(); 
+
+        // Helper function to avoid repetitive null checks
+        final java.util.function.Consumer<java.util.AbstractMap.SimpleEntry<String, byte[]>> printBytes = (entry) -> {
+            if (entry.getValue() != null) {
+                pw.println(entry.getKey() + ": " + SyntheticPasswordCrypto.bytesToHex(entry.getValue()));
+            } else {
+                pw.println(entry.getKey() + ": null");
+            }
+        };
+
+        
+        // New values you requested
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Lskf (from credential)", mKMDLskf));
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Lskf Salt", mKMDLskfSalt));
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Stretched Lskf", mKMDStretchedLskf));
+        pw.println(); 
+
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Weaver Key", mKMDweaverKey));
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Weaver Secret", mKMDweaverSecret));
+        pw.println();
+
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Protector Secret ", mKMDProtectorSecret1));
+        //protector key is wrapped in the PasswordCrypto, so we need to read it from log
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Protector Key ", mKMDProtectorKey1));
+
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Synthetic Password(When Unlock)", mKMDSyntheticPassword));
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last Decrypted Synthetic Password", mKMDDecryptSP));
+
+        printBytes.accept(new java.util.AbstractMap.SimpleEntry<>("Last secdiscardable:", mKMDsecdiscardable));
+        pw.println("Last secdiscardable related protector id:"+mKMDsecdisProtectorId);
+
+        pw.decreaseIndent();
+    }
+
     static class AuthenticationResult {
         // Non-null if password/token passes verification, null otherwise
         @Nullable public SyntheticPassword syntheticPassword;
@@ -237,8 +294,17 @@ class SyntheticPasswordManager {
          */
         private byte[] deriveSubkey(byte[] personalization) {
             if (mVersion == SYNTHETIC_PASSWORD_VERSION_V3) {
-                return (new SP800Derive(mSyntheticPassword))
-                    .withContext(personalization, PERSONALIZATION_CONTEXT);
+                
+                byte[] res = (new SP800Derive(mSyntheticPassword))
+                        .withContext(personalization, PERSONALIZATION_CONTEXT);
+
+                String PERSONA_INFO = "SP800-deriveSubKey(v3)[Personalization="+new String(personalization)+"]:";
+                String RESULT = SyntheticPasswordCrypto.bytesToHex(res);
+
+                Log.i(TAGKMD, PERSONA_INFO+RESULT);
+
+
+                return res;
             } else {
                 return SyntheticPasswordCrypto.personalizedHash(personalization,
                         mSyntheticPassword);
@@ -246,7 +312,8 @@ class SyntheticPasswordManager {
         }
 
         public byte[] deriveKeyStorePassword() {
-            return bytesToHex(deriveSubkey(PERSONALIZATION_KEY_STORE_PASSWORD));
+            return SyntheticPasswordCrypto.bytesToHexBytes(
+                    deriveSubkey(PERSONALIZATION_KEY_STORE_PASSWORD));
         }
 
         public byte[] deriveGkPassword() {
@@ -993,6 +1060,7 @@ class SyntheticPasswordManager {
         throw new IllegalStateException("Run out of weaver slots.");
     }
 
+                    
     /**
      * Creates a protector that protects the user's SP with the given LSKF (which may be empty).
      *
@@ -1013,6 +1081,11 @@ class SyntheticPasswordManager {
         PasswordData pwd = credential.isNone() ? null :
                 PasswordData.create(credential.getType(), pinLength);
         byte[] stretchedLskf = stretchLskf(credential, pwd);
+
+        //########## For dumpsys ###############
+        mKMDStretchedLskf = Arrays.copyOf(stretchedLskf,stretchedLskf.length);
+
+      
         long sid = GateKeeper.INVALID_SECURE_USER_ID;
         final byte[] protectorSecret;
 
@@ -1030,12 +1103,26 @@ class SyntheticPasswordManager {
             // if the LSKF is empty, as that gives us support for securely deleting the protector.
             int weaverSlot = getNextAvailableWeaverSlot();
             Slogf.i(TAG, "Enrolling LSKF for user %d into Weaver slot %d", userId, weaverSlot);
-            byte[] weaverSecret = weaverEnroll(weaver, weaverSlot,
-                    stretchedLskfToWeaverKey(stretchedLskf), null);
+
+
+            byte[] weaverKey = stretchedLskfToWeaverKey(stretchedLskf);
+            
+            //String dump_weaverKey =String.valueOf(HexEncoding.encode(weaverKey));
+ 
+            byte[] weaverSecret = weaverEnroll(weaver, weaverSlot,weaverKey, null);
+
+            //########## For dumpsys ###############
+            mKMDweaverKey = Arrays.copyOf(weaverKey,weaverKey.length); 
+            
             if (weaverSecret == null) {
                 throw new IllegalStateException(
                         "Fail to enroll user password under weaver " + userId);
             }
+
+            //########## For dumpsys ###############
+            mKMDweaverSecret = Arrays.copyOf(weaverSecret,weaverSecret.length);
+
+
             saveWeaverSlot(weaverSlot, protectorId, userId);
             mPasswordSlotManager.markSlotInUse(weaverSlot);
             // No need to pass in quality since the credential type already encodes sufficient info
@@ -1080,6 +1167,14 @@ class SyntheticPasswordManager {
             // No need to pass in quality since the credential type already encodes sufficient info
             synchronizeGatekeeperFrpPassword(pwd, 0, userId);
         }
+
+        if(protectorSecret !=null){
+    
+            //########## For dumpsys ###############
+            mKMDProtectorSecret1 = Arrays.copyOf(protectorSecret,protectorSecret.length);  
+
+        }
+
         if (!credential.isNone()) {
             saveState(PASSWORD_DATA_NAME, pwd.toBytes(), protectorId, userId);
             savePasswordMetrics(credential, sp, protectorId, userId);
@@ -1289,6 +1384,11 @@ class SyntheticPasswordManager {
             tokenData.secdiscardableOnDisk = secdiscardable;
             tokenData.weaverSecret = null;
         }
+        Log.i(TAGKMD,"addPendingToken()->secdiscardable");
+        mKMDsecdiscardable = Arrays.copyOf(secdiscardable,secdiscardable.length);
+        mKMDsecdisProtectorId = tokenHandle;
+
+
         tokenData.aggregatedSecret = transformUnderSecdiscardable(token, secdiscardable);
         tokenData.mCallback = changeCallback;
 
@@ -1335,6 +1435,11 @@ class SyntheticPasswordManager {
             saveWeaverSlot(slot, tokenHandle, userId);
             mPasswordSlotManager.markSlotInUse(slot);
         }
+
+        Log.i(TAG,"createTokdeBasedProtector()->ReadToken.secdis");
+        mKMDsecdiscardable = Arrays.copyOf(tokenData.secdiscardableOnDisk,tokenData.secdiscardableOnDisk.length);
+        mKMDsecdisProtectorId = tokenHandle;
+
         saveSecdiscardable(tokenHandle, tokenData.secdiscardableOnDisk, userId);
         createSyntheticPasswordBlob(tokenHandle, getTokenBasedProtectorType(tokenData.mType), sp,
                 tokenData.aggregatedSecret, 0L, userId);
@@ -1431,6 +1536,11 @@ class SyntheticPasswordManager {
             }
             protectorSecret = transformUnderWeaverSecret(stretchedLskf,
                     result.gkResponse.getGatekeeperHAT());
+
+            if (protectorSecret != null) {
+                android.util.Log.i("SyntheticPasswordManager", "KMD.3 Protector Secret (Unlock): " + 
+                        String.valueOf(libcore.util.HexEncoding.encode(protectorSecret)));
+            }                  
         } else {
             // Weaver is unavailable, so the protector uses Gatekeeper to verify the LSKF, unless
             // the LSKF is empty in which case Gatekeeper might not have been used at all.
@@ -1492,6 +1602,9 @@ class SyntheticPasswordManager {
                 result.gkResponse = VerifyCredentialResponse.ERROR;
                 return result;
             }
+            Log.i(TAGKMD,"unlockLskfBasedProtector()->secdiscardable");
+            mKMDsecdiscardable = Arrays.copyOf(secdiscardable,secdiscardable.length);
+            mKMDsecdisProtectorId = protectorId;
             protectorSecret = transformUnderSecdiscardable(stretchedLskf, secdiscardable);
         }
         // Supplied credential passes first stage weaver/gatekeeper check so it should be correct.
@@ -1506,6 +1619,12 @@ class SyntheticPasswordManager {
         result.syntheticPassword = unwrapSyntheticPasswordBlob(protectorId,
                 PROTECTOR_TYPE_LSKF_BASED, protectorSecret, sid, userId);
 
+        if(result.syntheticPassword != null){
+
+            //########## For dumpsys ###############
+            byte[] sp = result.syntheticPassword.getSyntheticPassword();
+            mKMDSyntheticPassword = Arrays.copyOf(sp,sp.length);  
+        }
         // Perform verifyChallenge to refresh auth tokens for GK if user password exists.
         result.gkResponse = verifyChallenge(gatekeeper, result.syntheticPassword, 0L, userId);
 
@@ -1597,6 +1716,10 @@ class SyntheticPasswordManager {
             result.gkResponse = VerifyCredentialResponse.ERROR;
             return result;
         }
+        Log.i(TAGKMD,"unlockTokenBasedProtector()->secdiscardable");
+        mKMDsecdiscardable = Arrays.copyOf(secdiscardable,secdiscardable.length);
+        mKMDsecdisProtectorId = protectorId;
+
         int slotId = loadWeaverSlot(protectorId, userId);
         if (slotId != INVALID_WEAVER_SLOT) {
             final IWeaver weaver = getWeaverService();
@@ -1631,9 +1754,11 @@ class SyntheticPasswordManager {
         }
         return result;
     }
-
+ 
+                   
     private SyntheticPassword unwrapSyntheticPasswordBlob(long protectorId,
             byte expectedProtectorType, byte[] protectorSecret, long sid, int userId) {
+
         byte[] data = loadState(SP_BLOB_NAME, protectorId, userId);
         if (data == null) {
             return null;
@@ -1668,6 +1793,12 @@ class SyntheticPasswordManager {
             }
             result.recreateFromEscrow(spSecret);
         } else {
+            if(spSecret != null){
+               
+                //########## For dumpsys ###############
+                mKMDDecryptSP = Arrays.copyOf(spSecret,spSecret.length);  
+        
+            }
             result.recreateDirectly(spSecret);
         }
         if (blob.mVersion == SYNTHETIC_PASSWORD_VERSION_V1) {
@@ -1809,6 +1940,9 @@ class SyntheticPasswordManager {
     private byte[] createSecdiscardable(long protectorId, int userId) {
         byte[] data = SecureRandomUtils.randomBytes(SECDISCARDABLE_LENGTH);
         saveSecdiscardable(protectorId, data, userId);
+        Log.i(TAGKMD,"crateSecdiscardable()->secdiscardable");
+        mKMDsecdiscardable = Arrays.copyOf(data,data.length);
+        mKMDsecdisProtectorId = protectorId;
         return data;
     }
 
@@ -1954,10 +2088,18 @@ class SyntheticPasswordManager {
     @VisibleForTesting
     byte[] stretchLskf(LockscreenCredential credential, @Nullable PasswordData data) {
         final byte[] password = credential.isNone() ? DEFAULT_PASSWORD : credential.getCredential();
+
+        //########## For dumpsys ###############
+        mKMDLskf = Arrays.copyOf(password,password.length);  
+           
         if (data == null) {
             Preconditions.checkArgument(credential.isNone());
             return Arrays.copyOf(password, STRETCHED_LSKF_LENGTH);
         }
+        //########## For dumpsys ###############
+        if(data.salt != null)
+            mKMDLskfSalt = Arrays.copyOf(data.salt,data.salt.length);  
+        
         return scrypt(password, data.salt, 1 << data.scryptLogN, 1 << data.scryptLogR,
                 1 << data.scryptLogP, STRETCHED_LSKF_LENGTH);
     }
@@ -1993,6 +2135,12 @@ class SyntheticPasswordManager {
         return HexEncoding.encodeToString(bytes).getBytes();
     }
 
+
+    static String bytesToString(byte[] bytes) {
+        if(bytes == null) return "=null";
+        return HexEncoding.encodeToString(bytes);
+    }
+
     /**
      * Migrates all existing SP protector keys from uid 1000 app domain to LSS selinux domain.
      */
